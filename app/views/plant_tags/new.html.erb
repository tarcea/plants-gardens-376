Create a tag for <%= @plant.name %>

<%= simple_form_for [@plant, @plant_tag] do |form| %>
  <%= form.input :tag_id, collection: Tag.all, input_html: { multiple: true, class: "select2" }, include_hidden: false %>
  <%# form.association :tag %>
  <%= form.submit %>
<% end %>
